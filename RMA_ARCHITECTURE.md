# RMA System Architecture & Flow

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         Frontend (React)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ RMASubmit.jsx│  │  RMAList.jsx │  │ RMAAdmin.jsx │          │
│  │   (User)     │  │   (User)     │  │   (Admin)    │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                  │                  │                   │
│         └──────────────────┼──────────────────┘                   │
│                            │                                      │
└────────────────────────────┼──────────────────────────────────────┘
                             │
                             │ HTTP Requests
                             │ (Bearer Token)
                             │
┌────────────────────────────┼──────────────────────────────────────┐
│                            ▼                                       │
│                   Backend API (Express)                           │
├───────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌────────────────────────────────────────────────────────┐      │
│  │              Authentication Middleware                   │      │
│  │              (JWT Verification)                         │      │
│  └────────────────┬───────────────────────────────────────┘      │
│                   │                                               │
│  ┌────────────────▼───────────────────────────────────────┐      │
│  │                 RMA Routes                              │      │
│  │  • POST   /api/rma/submit                             │      │
│  │  • GET    /api/rma/my-requests                        │      │
│  │  • GET    /api/rma/admin/all          (Admin)         │      │
│  │  • PUT    /api/rma/admin/update/:id   (Admin)         │      │
│  └────────────────┬───────────────────────────────────────┘      │
│                   │                                               │
│  ┌────────────────▼───────────────────────────────────────┐      │
│  │              File Upload Handler                        │      │
│  │              (Multer Middleware)                        │      │
│  │  • Validate file type (image/PDF)                      │      │
│  │  • Check file size (max 10MB)                          │      │
│  │  • Save to uploads/rma/                                │      │
│  └────────────────┬───────────────────────────────────────┘      │
│                   │                                               │
└───────────────────┼───────────────────────────────────────────────┘
                    │
┌───────────────────▼───────────────────────────────────────────────┐
│                     Database (MongoDB)                             │
├────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │                     RMA Collection                         │    │
│  │  {                                                        │    │
│  │    _id: ObjectId                                          │    │
│  │    serialNumber: String                                   │    │
│  │    invoiceNumber: String                                  │    │
│  │    poNumber: String                                       │    │
│  │    reason: String                                         │    │
│  │    description: String                                    │    │
│  │    status: String (pending/approved/rejected/...)         │    │
│  │    attachments: [{filename, url, type, uploadedAt}]       │    │
│  │    submittedBy: ObjectId (ref: User)                      │    │
│  │    processedBy: ObjectId (ref: User)                      │    │
│  │    adminNotes: String                                     │    │
│  │    createdAt: Date                                        │    │
│  │    updatedAt: Date                                        │    │
│  │  }                                                        │    │
│  └──────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

## 🔄 User Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         User Journey                                 │
└─────────────────────────────────────────────────────────────────────┘

1. User Login
   │
   ├──► Navigate to Dashboard
   │
   └──► Click "RMA" in Sidebar
         │
         ▼
   ┌─────────────────┐
   │   RMA List      │  ← Shows user's previous RMA requests
   │   (/rma/list)   │
   └────────┬────────┘
            │
            │ Click "New RMA Request"
            │
            ▼
   ┌─────────────────┐
   │  RMA Submit     │
   │  (/rma/submit)  │  ← Fill form + Upload files
   └────────┬────────┘
            │
            │ Submit
            │
            ▼
   ┌─────────────────┐
   │  POST Request   │
   │  to Backend     │  ← API call with FormData
   └────────┬────────┘
            │
            │ Success
            │
            ▼
   ┌─────────────────┐
   │  Redirect to    │
   │  RMA List       │  ← See new request with "pending" status
   └────────┬────────┘
            │
            │ Track Status
            │
            ▼
   ┌─────────────────┐
   │  View Details   │  ← Modal with full info + attachments
   │  & Status       │
   └─────────────────┘
```

## 👨‍💼 Admin Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Admin Journey                                │
└─────────────────────────────────────────────────────────────────────┘

1. Admin Login
   │
   ├──► Navigate to /rma/admin (or add link in admin menu)
   │
   ▼
┌──────────────────────┐
│   RMA Admin Panel    │  ← Dashboard with statistics
│   (/rma/admin)       │     • Total Requests
└──────────┬───────────┘     • Pending Count
           │                 • Approved Count
           │                 • Rejected Count
           │
           ├──► Search by Serial/Invoice/PO
           │
           ├──► Filter by Status
           │
           └──► View All Requests Table
                 │
                 │ Click "View" on a request
                 │
                 ▼
           ┌─────────────────┐
           │  Request Details │
           │  Modal          │  ← Full details + attachments
           └────────┬────────┘
                    │
                    │ Update Status
                    │
                    ▼
           ┌─────────────────┐
           │  Select Action: │
           │  • Approve      │
           │  • Process      │  ← Based on current status
           │  • Reject       │
           │  • Complete     │
           └────────┬────────┘
                    │
                    │ Add Admin Notes
                    │
                    ▼
           ┌─────────────────┐
           │  PUT Request    │
           │  to Backend     │  ← Update status + notes
           └────────┬────────┘
                    │
                    │ Success
                    │
                    ▼
           ┌─────────────────┐
           │  Status Updated │  ← User sees update in their list
           │  Refresh List   │
           └─────────────────┘
```

## 📊 Status State Machine

```
┌─────────────────────────────────────────────────────────────────────┐
│                      RMA Status Flow                                 │
└─────────────────────────────────────────────────────────────────────┘

                    ┌──────────┐
                    │ PENDING  │  ← Initial status when submitted
                    └────┬─────┘
                         │
         ┌───────────────┼───────────────┐
         │               │               │
         ▼               ▼               ▼
    ┌─────────┐    ┌──────────┐    ┌──────────┐
    │REJECTED │    │APPROVED  │    │PROCESSING│
    └─────────┘    └────┬─────┘    └────┬─────┘
         │              │               │
         │              └───────┬───────┘
         │                      │
         │                      ▼
         │              ┌──────────────┐
         │              │ PROCESSING   │
         │              └──────┬───────┘
         │                     │
         │                     ▼
         │              ┌──────────────┐
         │              │  COMPLETED   │  ← Final success state
         │              └──────────────┘
         │
         └─────────► [END]  ← Final rejection state


Status Transitions:
• pending → approved (Admin approves)
• pending → rejected (Admin rejects)
• pending → processing (Admin starts processing)
• approved → processing (Admin starts processing)
• processing → completed (Admin completes)
```

## 🔐 Authentication Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                     Authentication Flow                              │
└─────────────────────────────────────────────────────────────────────┘

User/Admin Login
      │
      ▼
┌──────────────┐
│ POST /login  │
└──────┬───────┘
       │
       │ Success
       │
       ▼
┌────────────────────────┐
│ Store in localStorage: │
│ • authToken            │  ← JWT token
│ • userInfo (name, email, role)
└──────┬─────────────────┘
       │
       │ Every RMA Request
       │
       ▼
┌─────────────────────────────┐
│ Add to Request Headers:     │
│ Authorization: Bearer <token>
└──────┬──────────────────────┘
       │
       ▼
┌──────────────────────┐
│ Backend Middleware   │
│ • Verify JWT         │
│ • Extract user info  │
│ • Check admin role   │  ← For admin endpoints
└──────┬───────────────┘
       │
       ├──► Valid → Process Request
       │
       └──► Invalid → 401/403 Error
```

## 📁 File Upload Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                      File Upload Process                             │
└─────────────────────────────────────────────────────────────────────┘

User selects files
      │
      ▼
┌──────────────────┐
│ Frontend         │
│ Validation:      │
│ • File type      │  ← Only images & PDFs
│ • File size      │  ← Max 10MB each
└──────┬───────────┘
       │
       │ Create FormData
       │
       ▼
┌──────────────────────┐
│ FormData Object:     │
│ • serialNumber       │
│ • invoiceNumber      │
│ • poNumber           │
│ • reason             │
│ • description        │
│ • attachments[]      │  ← Multiple files
└──────┬───────────────┘
       │
       │ POST to /api/rma/submit
       │
       ▼
┌────────────────────────┐
│ Backend Multer:        │
│ • Parse multipart/form-data
│ • Validate file types  │
│ • Check file sizes     │
│ • Generate unique names│
│ • Save to disk         │  ← uploads/rma/
└──────┬─────────────────┘
       │
       │ Save to database
       │
       ▼
┌──────────────────────────┐
│ Database Record:         │
│ attachments: [           │
│   {                      │
│     filename: "...",     │
│     url: "/uploads/...", │
│     type: "image",       │
│     uploadedAt: Date     │
│   }                      │
│ ]                        │
└──────┬───────────────────┘
       │
       │ Return response
       │
       ▼
┌──────────────────────────┐
│ Frontend displays:       │
│ • File previews          │
│ • Download links         │
│ • Thumbnails             │
└──────────────────────────┘
```

## 🔄 Data Flow - Submit RMA

```
RMASubmit Component
      │
      │ User fills form
      │
      ▼
┌──────────────────────┐
│ formData State:      │
│ {                    │
│   serialNumber: "",  │
│   invoiceNumber: "",│
│   poNumber: "",      │
│   reason: "",        │
│   description: ""    │
│ }                    │
└──────┬───────────────┘
       │
       │ User uploads files
       │
       ▼
┌──────────────────────┐
│ files State: []      │
│ filePreviews: []     │
└──────┬───────────────┘
       │
       │ Submit button clicked
       │
       ▼
┌──────────────────────────────┐
│ Validation:                  │
│ ✓ Required fields filled     │
│ ✓ At least 1 file uploaded   │
└──────┬───────────────────────┘
       │
       │ Create FormData
       │
       ▼
┌──────────────────────────────┐
│ const submitData = new FormData()
│ submitData.append('serialNumber', ...)
│ submitData.append('invoiceNumber', ...)
│ submitData.append('poNumber', ...)
│ submitData.append('reason', ...)
│ submitData.append('description', ...)
│ files.forEach(file => {      │
│   submitData.append('attachments', file)
│ })                           │
└──────┬───────────────────────┘
       │
       │ fetch() API call
       │
       ▼
┌──────────────────────────────┐
│ POST ${API_URL}/api/rma/submit
│ Headers: {                   │
│   Authorization: Bearer <token>
│ }                            │
│ Body: submitData             │
└──────┬───────────────────────┘
       │
       ├──► Success → navigate('/rma/list')
       │              + toast.success()
       │
       └──► Error → toast.error()
```

## 📱 Component Hierarchy

```
App.jsx
  │
  ├── Sidebar.jsx (includes RMA link)
  │
  └── Router
       │
       ├── /rma/submit → RMASubmit.jsx
       │                   │
       │                   ├── Form inputs
       │                   ├── File upload area
       │                   ├── File previews
       │                   └── Submit button
       │
       ├── /rma/list → RMAList.jsx
       │                 │
       │                 ├── RMA cards list
       │                 ├── Status badges
       │                 ├── Progress bars
       │                 └── Detail modal
       │
       └── /rma/admin → RMAAdmin.jsx
                         │
                         ├── Statistics cards
                         ├── Search & filter
                         ├── RMA table
                         └── Detail modal with actions
```

---

This diagram gives you a complete visual overview of how the RMA system works from frontend to backend to database! 🎨
